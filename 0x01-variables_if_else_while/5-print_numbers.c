#include <stdio.h>
#include <stdlib.h>
#include <time.h>
/**
 * main - Start the program
 * Description: print all nom
 * Return: 0 if right
 */
int main(void)
{
for (int i = 0, i < 10, i++)
printf("%d", i);
printf("\n");
return (0);
}
